# Base Pipeline Configuration
version: "local"
save_folder: "results"
exp_name: "experiment_001"

# Evaluation settings
save_metric: true
metric_names:
  - "psnr"
  - "ssim"
  - "niqe"
  - "brisque"

# Image saving
save_img: true
img_norm: false

# Dataloader settings (test)
bs_test: 1
nw_test: 0
pin_memory: false

# Mode
_mode: "single_mode"